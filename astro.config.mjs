// @ts-check
import { defineConfig } from "astro/config";

// Published at https://kostyrko.github.io/trailroam-landingpage/
// `site` is used for canonical URL / Open Graph tags.
// `base` must match the GitHub Pages project path (/<repo-name>/).
export default defineConfig({
  site: "https://kostyrko.github.io",
  base: "/trailroam-landingpage",
});
